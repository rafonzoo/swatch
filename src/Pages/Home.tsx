// import { useCustomer } from 'Utils/hook';

function Home () {
  // const { account } = useCustomer();

  return (
    <div className='safearea' style={{ paddingTop: 75, textAlign: 'center' }}>
      <p style={{ fontSize: 70, fontWeight: 600, margin: '2rem auto' }}>d_(^-^)_b</p>
      Homepage isn`t created yet. Navigate to shop page through the navigation above.
    </div>
  );
}

export default Home;
